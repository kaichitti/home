# WebBrowse Linux - apk packages
# One package per line. Lines starting with # are ignored.

# --- Base system ---
alpine-base
alpine-baselayout
busybox
openrc
linux-lts
# linux-firmware-none is an empty package that satisfies linux-lts's
# cmd:linux-firmware-any dependency without pulling ~1 GB of firmware
# blobs. Required to keep the ISO under the 600 MB target.
linux-firmware-none
mkinitfs

# --- Bootloader / ISO tooling ---
syslinux

# --- X server (minimal) ---
xorg-server
xinit
xf86-input-libinput
xf86-video-vesa
xf86-video-fbdev

# --- Window manager ---
openbox

# --- Launcher runtime ---
python3
python3-tkinter

# --- Browsers ---
firefox-esr
chromium

# --- Fonts (Japanese-capable) ---
font-noto
font-noto-cjk

# --- Networking (NetworkManager excluded by design) ---
wpa_supplicant
dhcpcd
iw

# --- User / auth tools ---
shadow
util-linux
sudo

# --- Misc runtime ---
tzdata
ca-certificates
