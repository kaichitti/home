# ぷらチャット 💬

ルーム型チャットサイト「ぷらチャット」です。
好きなチャットルームにニックネームだけで入室でき、部屋の作成も自由。登録不要・完全無料のひまつぶしチャットです。

昔の携帯・CGIチャットと同じ「サーバーサイドHTML＋フォーム送信」で動く超軽量設計
（クライアントJSはポップアップ用の `window.open` のみで、無くても全機能動作）。
iOS 4 / Android 1.6 のほか、ニンテンドー3DS・Wii U・PS3以降のゲーム機ブラウザでも動作します。
ポップアップが開けないブラウザでは自動的に通常のページ遷移になります。
デザインはライトブルー基調の無骨な旧式スタイルです。

## 2つの実装

| 版 | 場所 | 用途 |
| --- | --- | --- |
| **Perl CGI版** | `xrea/` | **本番用。** XREAなどの共用レンタルサーバーに設置して動かします |
| Node.js版 | `server.js` | ローカルでの動作確認・開発用 |

XREAは常駐プログラム（デーモン）が禁止で、30秒／CPU15%を超えるタスクは強制終了
されるため、**Node.js版はXREAでは動作しません。** 共用サーバーで運用する場合は
Perl CGI版を使ってください。設置手順は **[xrea/DEPLOY-XREA.md](xrea/DEPLOY-XREA.md)** にあります。

Perl CGI版はデータをファイルに保存するため、**再起動してもチャットの内容が消えません**。
Perlのコアモジュールのみで動くのでCPANの追加インストールは不要です。

## 特徴

### チャット
- **登録不要・無料** — TOPページで名前を設定して部屋を選ぶだけ。未設定なら「ゲスト＋乱数4桁（1〜9999）」が自動で付く
- **名前は全部屋共通** — TOPページでのみ設定・変更（入室時の入力なし）。変更は入室中の全部屋・個人チャットに即反映
- **名前の色** — 明るめ16色のカラーパレット（色付きセル＋ラジオボタン）から**見たままの色**で選択。`bgcolor` 属性ベースなので旧端末でもJSなしで色が見える
- **タブ移動** — TOP・入室中の部屋・個人チャットをタブで行き来。**5部屋まで同時入室**できる
- **連番ルームID** — 部屋はNo.1から連番で、`/?room=5` のようなURLで直接アクセス可能
- **入室前の確認画面** — 部屋の説明文・人数・入室制限を確認してから「入室する/やめる」を選べる
- **サイコロ** — 「2d6」「1d100+3」と発言するとダイスを振る
- **参加者一覧・ユーザー詳細（ポップアップ）** — 「詳細」でユーザーエージェント・**IPアドレス・プロバイダ（リモートホストの逆引き）** を開示
- **ログの自動更新(標準ON)** — チャット画面は2フレーム構成で、上のログだけが自動更新(初期値10秒、5/10/30秒/OFFを選択可)され、下の入力欄は更新されないので入力中の文字が消えない。フレーム非対応ブラウザには手動更新の「フレームなし版」を用意

### 個人チャット
- **1対1の個人チャット** — 部屋の参加者一覧・ユーザー詳細から「個人チャット」で開始。**同じトークルームにいる相手とだけ**開始・送信できる（過去のやり取りは相手が退室後も閲覧可）
- **新着表示** — 受信するとタブに「個人チャット(新着N)」と赤く表示。開くと既読
- **無視（ブロック）** — 受け取りたくない相手は「無視する」ボタンでブロック。相手は送信できなくなる（「無視解除」で復帰）

### 部屋の作成と管理
- **部屋の作成** — TOPの「部屋を作る」ボタンから専用ページへ移動して作成。部屋名・説明文・定員（**2〜50人**）・管理パスワード（必須）を設定
- **鍵付き個室** — 入室パスワードを設定するとパスワードなしでは入室不可（デフォルトは誰でも入室可）。**管理パスワードと同一の入室パスワードは設定不可**
- **部屋の管理**（管理パスワードでログイン）
  - 部屋の削除（**削除前に確認ダイアログを表示**）
  - ユーザーのアクセス禁止（強制退室＋IP・ユーザー単位で再入室ブロック、解除も可能）
  - 画像投稿のON/OFF切り替え（ONの部屋では画像URL発言がインライン表示される）

### サイトの運営機能
- **インフォメーション** — `information.txt` の内容をTOPと `/info` に掲示（1行1件）
- **管理者に連絡** — 連絡フォームの内容を `contact.log` に記録

## 技術構成

| 層 | Perl CGI版（本番） | Node.js版（開発用） |
| --- | --- | --- |
| サーバー | Perl 5 CGI（コアモジュールのみ・CGI.pm不使用） | Node.js + Express |
| フロントエンド | HTML 4.01 + 最小限のCSS。WebSocket不使用・JSはポップアップのみ | 同左 |
| セッション | Cookie（sid）＋ファイル | Cookie（sid）＋メモリ |
| データ保存 | ファイル（`flock`で排他制御・再起動しても残る） | メモリのみ（再起動で消える） |
| 掃除処理 | cron（`gc.pl`）＋リクエスト時の確率実行 | `setInterval` |

## 起動方法

### 本番（XREAなどの共用サーバー）

**[xrea/DEPLOY-XREA.md](xrea/DEPLOY-XREA.md)** の手順に従ってファイルを配置してください。
アップロードとパーミッション設定だけで動きます。

### ローカルでの動作確認（Node.js版）

```bash
npm install
npm start
```

ブラウザで <http://localhost:3000> を開きます。
動作確認にはブラウザを2つ（またはシークレットウィンドウ）開いて、別々の名前で同じ部屋に入室してください。

ポートは環境変数 `PORT`、更新情報は `information.txt` を編集してください。
リバースプロキシの後ろで動かす場合のみ `TRUST_PROXY=1` を設定してください
（無条件に信用するとIPを詐称できてしまうため、既定では無効です）。

> Node.js版はデータをメモリに保持するため、再起動で内容が消えます。
> また、レート制限やCSRF対策はPerl CGI版のみに実装されています。
> 公開して運用する場合はPerl CGI版を使ってください。

## セキュリティ対策（Perl CGI版）

IP偽装の防止（`X-Forwarded-For` を信用しない）、CSRFトークン、Cookieの
`HttpOnly`／`SameSite`／`Secure`、パスワードのソルト付きSHA-256保存、管理画面の
トークン化、レート制限（発言・部屋作成・パスワード試行を別枠で管理）、XSS・
パストラバーサル・ヘッダインジェクション対策、`flock` による排他制御を実装しています。
詳細は [xrea/DEPLOY-XREA.md](xrea/DEPLOY-XREA.md) の「組み込み済みのセキュリティ対策」を参照してください。

## 注意

IPアドレスとプロバイダの開示は昔のチャットサイトの再現です。日本で一般公開する場合は
プライバシーポリシーの掲示を推奨します。NGワードフィルタ・通報機能は含まれていないため、
公開規模に応じて追加を検討してください。
