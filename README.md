# ヒマチャット風 💬

[himachat.jp（ヒマチャット）](https://himachat.jp/) 風の、匿名ひまつぶしチャットサイトです。
ニックネームを入力するだけで、オンライン中の誰かとランダムにマッチングして1対1チャットが始まります。

## 特徴

- **登録不要・無料** — ニックネームを入力するだけですぐ開始
- **完全匿名** — アカウントも個人情報も不要
- **ランダムマッチング** — 待機中の相手と自動でペアリング
- **🎲 ランダム質問** — 会話に詰まったらボタン一つで話題を提供
- **入力中インジケーター** — 相手が入力中かリアルタイム表示
- **次の相手へ** — ワンクリックで会話を終えて新しい相手を探せる
- **オンライン人数表示** — 今何人がチャット中か一目でわかる

## 技術構成

| 層 | 技術 |
| --- | --- |
| サーバー | Node.js + Express + Socket.IO |
| フロントエンド | 素のHTML / CSS / JavaScript（ビルド不要） |
| データ保存 | なし（メッセージはメモリ上で中継するのみ・ログ非保存） |

## 起動方法

```bash
npm install
npm start
```

ブラウザで <http://localhost:3000> を開きます。
動作確認にはタブを2つ開いて、それぞれ別のニックネームで参加してください。

ポートは環境変数 `PORT` で変更できます。

## 仕組み

1. クライアントがニックネームを送って参加（`join`）
2. サーバーが待機キューから相手を探し、いなければキューに追加
3. マッチング成立で双方に `matched` を通知し、以後メッセージを1対1で中継
4. 「次の相手へ」または切断で相手に `partner left` を通知し、再マッチング可能に

メッセージはサーバーに保存されず、接続中の相手にそのまま中継されるだけです。

## 注意

ひまつぶし用のデモアプリです。本番運用する場合はレート制限・NGワードフィルタ・
通報機能などのモデレーション機構を追加してください。
