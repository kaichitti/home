# ヒマチャット風 💬

[himachat.jp（ヒマチャット）](https://himachat.jp/) 風の、ルーム型チャットサイトです。
好きなチャットルームにニックネームだけで入室でき、部屋の作成も自由。登録不要・完全無料のひまつぶしチャットです。

昔の携帯・CGIチャットと同じ「サーバーサイドHTML＋フォーム送信」だけで動く超軽量設計で、
**クライアントJavaScriptを一切使いません**。1ページ約4KBなので、iOS 4 / Android 1.6 /
ニンテンドー3DSといった古いブラウザでも動作します。

## 特徴

- **登録不要・無料** — 名前を入力して部屋を選ぶだけ
- **名前の色変更** — 黒・赤・青・緑・橙・紫・茶・桃・水・灰の10色から選択
- **ルーム型の複数人チャット** — 公式部屋のほか、誰でも自由に部屋を作成できる
- **参加者一覧と詳細表示** — 「詳細」を押すと相手のユーザーエージェント・入室時刻・最終アクセスを表示
- **サイコロ機能** — 「2d6」と発言すると6面ダイス2つを振る。「2d6+3」のような補正も可能
- **直近ログ表示** — 部屋ごとに直近100件の発言を保持（新しい発言が上）
- **自動更新の選択** — 手動更新が基本。希望すればメタリフレッシュで10秒/30秒の自動更新
- **自動退室** — 10分間操作がないと自動退室（昔のチャットと同じ仕様）

## 技術構成

| 層 | 技術 |
| --- | --- |
| サーバー | Node.js + Express（サーバーサイドHTMLレンダリングのみ） |
| フロントエンド | HTML 4.01 + 最小限のCSS。JavaScript・WebSocket不使用 |
| セッション | Cookie（sid）とメモリ上のセッション表 |
| データ保存 | メモリ上のみ（再起動で消える） |

## 起動方法

```bash
npm install
npm start
```

ブラウザで <http://localhost:3000> を開きます。
動作確認にはブラウザを2つ（またはシークレットウィンドウ）開いて、別々の名前で同じ部屋に入室してください。

ポートは環境変数 `PORT` で変更できます。

## 仕組み

1. トップページに部屋一覧（名前・説明・人数）を表示
2. 名前と色を選んで「入室」を押すとPOST → Cookieセッション発行 → チャット画面へ
3. 発言はフォームPOST。ページが再描画され最新ログが見える（=送信が更新を兼ねる）
4. 「2d6」形式の発言はサーバーがサイコロを振って結果をログに流す
5. 「参加者一覧」→「詳細」で相手のユーザーエージェントなどを表示
6. 誰もいなくなったユーザー作成部屋は約1分後に自動削除（公式部屋は残る）

## 注意

ひまつぶし用のデモアプリです。本番運用する場合はレート制限・NGワードフィルタ・
通報機能などのモデレーション機構を追加してください。
